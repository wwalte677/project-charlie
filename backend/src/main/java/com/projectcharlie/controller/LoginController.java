package com.projectcharlie.controller;

import org.springframework.web.bind.annotation.*;

@RestController
public class LoginController {
    // TODO: Add POST /login endpoint to validate credentials.
}
