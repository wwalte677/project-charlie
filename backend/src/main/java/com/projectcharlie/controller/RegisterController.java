package com.projectcharlie.controller;

import org.springframework.web.bind.annotation.*;

@RestController
public class RegisterController {
    // TODO: Add POST /register endpoint to create new users.
}
