package com.projectcharlie.controller;

import org.springframework.web.bind.annotation.*;

@RestController
public class VoteController {
    // TODO: Add POST /vote endpoint to record user votes once per event.
}
