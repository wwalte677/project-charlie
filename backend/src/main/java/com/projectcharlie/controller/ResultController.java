package com.projectcharlie.controller;

import org.springframework.web.bind.annotation.*;

@RestController
public class ResultController {
    // TODO: Add GET /results endpoint to retrieve vote counts per event.
}
